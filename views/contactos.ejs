<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Contactos - CRM Básico</title>
    <link rel="stylesheet" href="/css/styles.css?v=1.0.0">
</head>
<body>
    <div class="container">
        <h1>👥 Lista de Contactos</h1>
        <% if (message) { %>
            <div class="alert alert-success fade-in">✅ <%= message %></div>
        <% } %>
        <% if (error) { %>
            <div class="alert alert-error fade-in">❌ <%= error %></div>
        <% } %>
        <% if (contactos && contactos.length > 0) { %>
            <ul class="contact-list">
                <% contactos.forEach(function(contacto) { %>
                    <li class="contact-item fade-in">
                        <div class="contact-info">
                            <h4><%= contacto.nombre %></h4>
                            <p>📧 <%= contacto.correo %></p>
                            <% if (contacto.telefono) { %>
                                <p>📞 <%= contacto.telefono %></p>
                            <% } %>
                            <% if (contacto.empresa) { %>
                                <p>🏢 <%= contacto.empresa %></p>
                            <% } %>
                            <p>
                                <span class="status-badge <%= contacto.estado %>">
                                    <%= contacto.estado === 'prospecto' ? '🔍 Prospecto' : contacto.estado === 'cliente' ? '✅ Cliente' : '😴 Inactivo' %>
                                </span>
                            </p>
                            <p class="text-muted text-small">
                                Creado: <%= new Date(contacto.fecha_creacion).toLocaleDateString('es-ES', { year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %>
                            </p>
                        </div>
                        <div class="contact-actions">
                            <a href="/contactos/<%= contacto.id %>/editar" class="btn btn-outline btn-sm">✏️ Editar</a>
                            <form action="/contactos/<%= contacto.id %>/eliminar" method="POST" style="display: inline;">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <button type="submit" class="btn btn-danger btn-sm">🗑️ Eliminar</button>
                            </form>
                        </div>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <div class="text-center" style="padding: 40px;">
                <h3>📭 No hay contactos</h3>
                <p class="text-muted">Agrega tu primer contacto usando el formulario de arriba.</p>
            </div>
        <% } %>
        <a href="/" class="btn">Volver al inicio</a>
    </div>
</body>
</html>
